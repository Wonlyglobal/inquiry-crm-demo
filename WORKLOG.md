# CRM Work Log

## 2026-09-23 — AI 智能顾问第一阶段（生产）

- 目标与授权：项目负责人确认面向老板、销售主管、市场和销售四类角色建设营销/业务智能顾问，目标为提高有效询盘率、报价率和渠道投入效率；所有建议只读，必须人工确认后进入既有业务流程。
- 交付：增加一级入口“AI 智能顾问”，包含今日建议、营销顾问、销售顾问、每日简报、每周分析和建议效果六个视图；按角色和当前账号可见数据生成优先级建议，并逐条展示证据、判断逻辑、置信度、预期收益、风险与数据缺口。
- 人工闭环：支持采纳、修改后采纳、忽略、无效四类反馈；审计由独立的认证 Edge Function 以服务身份写入，市场只读账号无需获得数据库写权限。建议行动只打开现有工作台、市场任务、询盘、报价或看板，不直接修改客户、阶段、分配、价格或状态。
- 安全边界：沿用现有 RLS/角色数据范围；未扩大任何角色的客户可见范围；不自动发信、分配、改价、判定有效/无效、成交/丢单；本阶段规则建议在浏览器内基于已加载的可验证统计生成，不向外部 AI 发送新增客户数据；对话继续沿用现有受控 AI 数据助手及审计。
- 验证：浏览器已成功加载本地候选的登录页，无启动级脚本错误；定向安全、权限、反馈、侧边栏和启动回归 19/19 通过，`git diff --check` 通过。
- 部署与验收：PR #73 已合并（merge `f842ce499fd3b2c404f61d5daf437c5f5a915842`），主分支安全检查、全量回归和 GitHub Pages 发布成功；`crm-ai-advisor-feedback` Edge Function 已部署到生产。李铧燕生产会话确认“AI 智能顾问”作为工作台一级入口可见，独立页面显示六个视图、经营决策顾问角色文案、权限说明及三条基于当日可见数据的建议卡片；未点击反馈或执行任何业务写入。
- 剩余事项：每日 09:00 通知和每周一自动推送、跨设备持久化建议效果及建议结果归因仍需服务端调度/数据表后续实施，不能把当前六视图误述为已完成定时推送。
- 回滚：候选仅新增前端页面、定向测试和独立只写审计的 Edge Function；回退对应提交并停用该 Function 即可，不涉及业务数据或数据库结构回滚。

## 2026-09-23 — 官网接入监控移入系统设置（生产）

- 用户确认将偏技术运维的“官网实时接入监控”从市场任务中心移走。完整指标、明细、刷新和失败重试入口改为仅在“系统设置”二级区域显示；市场任务中心正常时不占空间，仅存在失败记录时显示简短异常提示并可跳转查看。
- 保持原 owner/市场角色可见范围和服务端重试授权，不修改官网表单、询盘、UTM、权限或业务数据；回退本次前端提交即可恢复旧布局。PR #71 已合并并通过安全检查及 Pages 发布；定向 2/2、全量回归 399/399、`git diff --check` 通过。李铧燕生产会话验收：市场任务中心完整监控不可见且无失败提示（当前 0 条失败），系统设置中完整监控可见并正常显示今日提交、成功、失败、平均/最大延迟和明细空态；未执行重试或业务写入。

## 2026-09-23 — 市场任务快捷按钮未选中样式（生产）

- 用户反馈“进入邮件分拣”并未被选中却使用深色底，容易被误认为当前页签。三个市场任务快捷入口统一使用无底色次级按钮；深色底继续只用于当前选中的导航、页签或筛选状态。
- 仅调整前端视觉层级，不修改邮箱、询盘、权限或业务数据；回退本次前端提交即可恢复。PR #69 已合并并通过安全检查及 Pages 发布；定向 3/3、全量回归 397/397、`git diff --check` 通过。李铧燕生产会话刷新后验收三个快捷入口 class 均为 `ghost`，未选中的“进入邮件分拣”不再显示深色底。

## 2026-09-23 — 登录账号与官方询盘邮箱职责分离（生产）

- 授权人与目标：项目负责人确认李铧燕的登录邮箱保持 `chloelee@wonlyglobal.com`，公司官方询盘邮箱为 `inquiry@wonlyglobal.com`，李铧燕需处理市场分拣并提交主管分配；执行人 Codex。
- 修复：运营管理员进入“市场任务中心”时不再出现只有标题的空页面；在不改变登录账号、个人邮箱连接或公共邮箱连接记录的前提下，允许 owner/运营管理员使用官方询盘的周期任务、邮件分拣、背调、官网接入监控和提交分配入口。页面明确说明登录账号与公共邮箱相互独立。
- 安全与权限：未修改账号邮箱、认证身份、邮箱凭据、历史邮件、询盘归属、RLS 或审批规则；市场人员仍只能完成既定初筛/登记流程，最终负责人分配仍走主管/owner 的现有受控流程。
- 验证与回滚：定向 10/10、全量回归 396/396、`git diff --check`、合并后安全检查与 Pages 发布均通过；PR #67 合并为 `463320088ffb439b94f38fab0fee01daf83e6b05`。李铧燕生产真实会话显示本人身份“李铧燕 / 运营经理”，市场任务中心明确使用 `inquiry@wonlyglobal.com`，并成功加载本月 7 封新邮件、5 封待分拣、1 条有效性待确认、1 条背调待完成及 2 条待主管分配；未执行任何业务写入。回退本次前端提交即可恢复旧页面，不需要数据库回滚。

## 2026-09-23 — 导入订单纳入当前 CRM 时间口径（生产）

- 授权人与目标：项目负责人要求将昨日已导入的 24 条订单明细计入“当前 CRM”，并按订单日期整理和筛选；执行人 Codex。
- 口径调整：当前 CRM 的销售额、销售额趋势与环比均纳入已核验导入订单；今日、本周、本月、本季度、本年度和全部历史均按 `ordered_at` 过滤。导入订单明细仍保留独立来源、原订单号和原业务员，不伪造询盘、成交审批或当前业务员归属；询盘量、有效率、响应率、成交率和渠道 ROI 不受影响。
- 展示：当前 CRM 中直接显示导入订单概览、按原业务员金额排行及按月份排序的订单趋势；不再自动把 owner 切换到“综合视图 + 全部历史”。“历史工程 / 订单”仍保留历史分析入口。
- 安全与回滚：未修改订单、客户、询盘、权限或审计数据，仅调整 owner 可见看板统计口径；回退本次前端提交即可恢复旧口径，不需要数据库回滚。
- 验证与部署状态：代码 PR #65 已合并（merge `f71e7b0611dd9c3d58a09e329048ba7195d34697`），安全检查与 Pages 生产发布成功；`git diff --check`、定向 4/4 和全量回归 394/394 通过。生产以李铧燕账号目视验收“当前 CRM + 本月”：销售额 `¥2,064,752 / US$308,407`，导入订单 3 个、明细 3 条、金额 206.48 万元，2026 年 9 月趋势与原业务员排行均正常；全部历史仍为 22 个唯一订单、24 条明细、1,169.43 万元。未执行任何数据写入、删除或归属变更。

## 2026-09-23 — 经营看板历史订单假 0 修复（生产）

- 问题定位：现场刷新后，当前 CRM 的询盘、团队、成员与已导入历史订单同时变空；前端对关键查询错误使用空数组继续渲染，导致网络或数据服务失败被误显示为真实 `¥0`。生产历史订单导入验收记录仍为 24 行、22 个唯一订单、1,169.42780212 万元，没有执行删除或回滚。
- 交付：询盘、阶段、成员或 owner 历史订单任一关键数据加载失败时终止本次渲染；首次失败显示“— / 数据未加载”和明确错误提示，不再展示假 0；同一页面已存在有效数据时保留上一次结果；自动重试一次并提供人工“重新加载”入口。
- 看板口径：owner 账号成功读取已核验历史订单后，首次加载默认切换为“综合视图 + 全部历史”，并增加可见的“全部历史”周期按钮；历史订单继续只计入综合销售额，不进入询盘转化、响应率或渠道 ROI。
- 安全边界：未修改订单、询盘、客户、权限、审计或导入记录；未把客户数据写入浏览器持久缓存；仍由既有 RLS 和 owner-only 历史订单权限控制读取。
- 验证：`git diff --check` 通过；新增加载失败防假 0 和关键错误中止回归；定向 3/3、全量 394/394 通过。
- 生产验收：PR #63 已合并（merge `a12a042`），CRM 安全检查和 GitHub Pages 发布成功；李铧燕登录态刷新后，系统自动选择“综合视图 + 全部历史”，现场显示 22 个唯一订单、24 条明细、1,169.43 万元，销售指标显示综合销售额 `¥11,694,278`，不再为 0。
- 回滚：回退 PR #63 即可恢复原展示；不涉及数据库回滚。剩余风险为外部网络/数据服务仍可能短时不可达，但此时页面会明确标记加载失败并保留上一次有效数据，不再把错误当成真实业务 0。

## 2026-09-23 — 邮件批量转询盘按钮选择态（待部署）

- 交付：邮件分拣中未选择邮件时，“一键转为询盘商机”使用无底色普通按钮并禁用；选择至少一封邮件后才切换为深色主按钮，清空选择或处理完成后恢复无底色状态。
- 安全边界：仅调整选择状态反馈；既有转换 RPC、角色权限、人工选择和处理流程不变，未创建或修改任何客户数据。
- 验证：新增回归覆盖初始禁用、选择后高亮及清空后复位。
- 回滚：回退本次前端提交即可；无需数据库回滚。

## 2026-09-22 — 侧边栏一级分类标记范围修复（待部署）

- 问题：一级分类标记错误地复用了全局待处理总数，导致“沟通协作、销售支持、管理治理”等多个分组显示相同数字。
- 修复：标记只查询并汇总当前分组内部可见的子菜单标记；没有待处理项的分组不显示红点，不改变任何通知、邮件或任务数据。
- 安全边界：仅修复前端展示范围，不修改通知读取权限、已读状态、业务数据或服务端统计。
- 回滚：回退本次前端提交即可恢复旧展示；无需数据库回滚。

## 2026-09-22 — 侧边栏信息架构、看板默认口径与资格核验入口精简（待部署）

- 交付：侧边栏按“工作台、客户与商机、沟通协作、销售支持、管理治理”五个一级分组折叠展示；自动展开当前页面所在分组、记忆用户展开状态，并在一级分组聚合待处理数量。角色操作指南保持最后一个独立入口。
- 看板：李铧燕等 owner 账号在存在已核验历史订单时，首次加载默认切换为“综合视图 + 本年度”，避免已导入订单因默认停留“当前 CRM”而显示为 0；不修改底层统计口径或订单数据。
- 资格核验：按用户要求移除询盘详情的“资格核验”可见页签；保留历史资格字段、AI 建议、角色权限、审计和服务端写入控制，避免删除既有证据或降低安全控制。需要恢复时可回退本次前端提交。
- 安全边界：仅调整导航、默认展示和入口；未扩大任何角色权限、数据可见范围，未修改客户数据、历史订单、资格记录或审计记录。
- 验证：浏览器模块语法检查通过；定向测试覆盖侧边栏分组、徽标聚合、角色指南位置、看板默认范围、资格入口移除及资格工作流保留。
- 部署状态：PR #59 已合并至 `main`，CRM 安全检查、全量回归与 GitHub Pages 生产发布均成功；线上源码已核验包含五组导航、owner 综合年度默认口径，且不再包含资格核验可见页签。由于页面数据与菜单受登录角色控制，后续由 owner 与销售账号各进行一次业务页面目视验收。

## 2026-09-22 — 大规模客户浏览与自发自收邮件辨识（生产）

- 交付：客户管理三层总览改为公司主列表与关联详情联动；公司支持关键词搜索、最近更新/名称排序和每页 10 家分页，选中公司后仅展示该公司的联系人与商机，关联记录超过 8 条时提示进入详情查看，避免 200 家公司一次性堆叠和渲染。
- 邮件辨识：继续以同步得到的原始 From/To 为准；当个人邮箱邮件的发件人与收件人均为当前绑定邮箱时，列表明确标注“本人自发自收”，不将其伪装为客户发件，也不改写历史邮件数据。
- 安全边界：沿用当前账号的数据库行级权限和已加载可见数据；未扩大客户可见范围，未修改客户、联系人、商机、邮件正文或归属数据，未调用外部 AI。
- 验证：浏览器脚本语法检查通过；定向测试 12/12、全量回归 385/385 通过；`git diff --check` 通过。
- 部署状态：PR #55 的两项必需检查通过后合并至 `main`；GitHub Pages 生产发布成功（merge `f13eb99`），线上静态资源已核验包含分页、搜索、关联筛选及“本人自发自收”标记。李铧燕账号的“市场任务中心”同期现场核验可见且可打开。
- 回滚：回退本次前端与测试提交即可；无数据库和业务数据回滚事项。

## 2026-09-22 — 经营看板顶部精简

- 交付：移除经营看板顶部“设置目标”“申请导出清单”“导出审批记录”三个入口，以及标题下方的“询盘效率、销售转化、渠道 ROI 与风险预警”说明。
- 安全边界：仅移除界面入口及对应事件绑定；未删除销售目标、受控导出、审批记录或审计数据，AI 数据助手内的受控导出流程保持不变。
- 验证：`node --test tests/*.test.mjs`，373 项全部通过；`git diff --check` 通过。
- 部署状态：已通过 PR #52 合并至 `main`，GitHub Pages 生产发布成功（merge `9bee791`）。
- 回滚：回退本次提交即可恢复上述看板入口和说明。

## 2026-09-22 — 多渠道客户管理第一阶段候选

- 授权：项目负责人确认 WhatsApp、微信、电话、展会、官网、邮件、社媒、拜访、经销商/客户/部门转介绍及销售自主开发等渠道纳入统一客户管理，并要求开始建设。
- 交付：侧边栏“客户资料库”升级为“客户管理”；增加公司—联系人—商机三层总览；统一“新建客户 / 商机”表单及完整渠道、联系方式、需求、金额和预计采购日期字段。
- 权限与归属：新增受控 RPC `create_customer_opportunity`。销售只能创建归属本人的商机；市场和老板创建后进入待主管分配；销售遇到其他业务员正在负责的相同客户时服务端拒绝，不自动抢占归属；销售主管不代替业务员创建客户。
- 数据与审计：公司、联系人、商机和营销来源在同一数据库事务内创建/复用；五分钟相同提交幂等；写入统一来源、主来源、营销触点和审计；没有调用外部 AI。
- 安全：匿名用户无执行权限；RPC 校验正式启用账号、角色、来源、最小字段和输入长度；客户正文不写入审计详情。未扩大基础表读取范围。
- 验证：前端脚本语法检查通过，`node --test tests/*.test.mjs` 共 376 项全部通过，`git diff --check` 通过。生产数据库 dry-run 因本机缺少连接文件未执行，且没有应用迁移。
- 部署状态：已通过 PR #52 发布生产；数据库 RPC 与字段已验收存在。本次发布未创建测试/真实客户，未改变现有归属。
- 回滚：发布后可先撤销新 RPC 执行权限并回退页面；新增列保留为空，不删除审计或业务数据。
- 后续：批量 Excel/CSV 安全导入、疑似重复审核队列、协作/移交、微信/WhatsApp 聊天导入及自动摘要仍属于下一阶段。
## 2026-09-22 · 日报日历、模拟数据与询盘入口优化（待部署）

- 将销售日报的浏览器原生日期选择器替换为 CRM 自有日历弹层，统一月份切换、选中态、今日入口、悬停与窄屏布局；同步收紧日报标题区与日期卡片的留白、圆角和边界。
- 仅对 `september-2026-manager-demo-v1` 批次、明确标记 `is_simulated=true` 的 9 月模拟日报补充非零且按日期变化的线索数、跟进数及详细业务内容；真实日报不更新，并记录审计日志。
- 从询盘详情移除重复的“触达规则”和“流水线”入口；触达合规仍由服务端发送策略强制执行，流水线改为询盘列表内展开查看。
- 修复报价列表列数回归，避免新增询盘操作列影响报价页面字段对齐。
- 验证：内联模块脚本语法检查通过；最终全量回归 380/380 通过；已随 PR #52 发布生产，模拟日报更新审计为 1 条且仅覆盖指定模拟批次。

## 2026-09-22 · 发货汇总 921 历史客户项目导入（生产）

- 授权人：用户；执行人：李铧燕（运营经理 / owner）；目标：将“发货汇总921”中已有正式 CRM 账号的业务员历史发货项目归入其个人历史客户视图，并更新经营看板历史与综合口径。
- 范围：仅导入刘海洋 9 条、李浩东 4 条、王大平 2 条、郑玉龙 2 条、陈敏慧 10 条，共 27 条；按用户最新要求，不创建牟啸宇、孙友、陈彬俏、佘唯唯账号，也不导入上述 4 人的 24 条订单。
- 安全控制：导入前核对 5 个账号均为唯一、启用、非测试、生产环境的销售账号；旧 ID 区间冲突数为 0；新增写入触发器按姓名精确映射 `owner_id`，不唯一或无匹配时整批失败；公共导入 RPC 仅 owner 可执行；审计只记录文件名、行数和归属策略，不保存客户正文。
- 前后状态：导入前目标 ID 冲突 0；导入后源文件记录 27 条、5 位负责人 `owner_id` 缺失均为 0、排除人员记录 0；历史工程看板由 904 条更新为 931 条，当前 CRM 询盘指标不受影响。
- 验证：生产审计 `legacy_customer_projects_imported` 记录行数 27；经营看板“历史工程”显示 931 个历史项目；按负责人聚合数量和金额与导入工作簿一致。
- 回滚：仅删除 `source_file='发货汇总921-CRM历史项目导入-5人.xlsx'` 且本批稳定旧 ID 的 27 条记录；保留审计，并另记回滚原因、执行人和结果。当前未执行回滚。
- 剩余风险：原表缺少可靠公司主体及联系人信息，因此本批按“历史发货项目（待补公司）”归档，不臆造公司或联系人；后续补齐主体前不用于外部联系或自动 AI 分析。
- 部署状态：生产数据与归属/审计保护已生效；通用客户 CSV 导入已随 PR #52 发布，服务端 RPC、界面入口及 500 行上限均已上线。

## 2026-09-22 · 此前排除的历史订单正式导入并计入看板

- 授权人：用户；执行人：李铧燕（运营经理 / owner）；目标：撤销此前对牟啸宇、孙友、陈彬俏、佘唯唯订单的导入排除，将源文件中的历史订单纳入经营看板。
- 导入范围：源文件 `发货汇总921.xlsx` 中上述 4 人共 24 行订单明细；核验为 22 个唯一订单号，其中 1 个订单号含 3 行物料明细。看板订单数按唯一订单号统计，金额按全部明细行累计，避免订单数重复放大。
- 安全设计：不创建未获授权的业务员账号，不伪造询盘或成交审批，不写入受“成交审核”约束的实时履约订单表；使用独立历史订单明细表，只有 owner 可读、可幂等导入或回滚。审计只记录文件哈希、行数、唯一订单数、金额和映射策略，不写客户、合同或订单正文。
- 看板口径：在“历史工程/综合视图”展示历史订单唯一订单数、明细数、金额、原业务员排名和月度趋势；“综合视图”的销售额为当前 CRM 已审核成交额加历史订单额。历史订单不进入询盘成交率、响应率或渠道 ROI。源表没有国家字段，保持未填写，不作推断。
- 回滚：调用 owner-only `rollback_historical_sales_order_import` 按源文件 SHA-256 删除本批明细，并另记回滚原因、执行人、删除行数和原批次汇总；不影响当前 CRM 询盘、正式履约订单和此前 27 条历史项目。
- 验证：内联模块脚本语法检查、`git diff --check` 和全量回归 388/388 已通过；新增定向测试覆盖 owner-only、RLS、幂等键、审计、回滚以及 24 行/22 单看板口径。
- 部署状态：2026-09-22 PostgreSQL 直连 dry-run 被远端中断后，改用 Supabase 官方 Management API 执行同一迁移；随后以李铧燕（运营经理 / owner）身份调用 owner-only 幂等导入 RPC。生产验收为 24 行、22 个唯一订单、1,169.42780212 万元、4 位原业务员，审计动作 `historical_sales_orders_imported` 已写入；原有 51 条测试/无效询盘仍保持隔离。前端已由 PR #57 合并，main 安全检查与 Pages 部署成功；线上源码验收已确认“历史工程 / 订单”、历史订单经营概览和综合销售额聚合逻辑生效。回滚方式为回退 PR #57，并调用 owner-only 回滚 RPC 删除本批哈希对应的 24 行。

## 2026-09-23 智能体世界正式接入候选

用户授权正式接入并提供WONLY金色Logo；Codex接入全站双世界切换、三色脑核、现有数据助手/证据建议和Logo下接CRM组合。新入口仅限已认证李铧燕精确身份+启用owner，未扩大数据权限。404回归及语法、合成浏览器入口/交互/撤销/窄屏检查通过。尚未部署；无数据库迁移、外部AI控制绕过或真实业务写入。详情docs/agent-world-integration.md。

## 2026-09-23 智能体信息简报与每日研究候选

用户确认按CRM分布自动确定研究顺序、发现竞品、每日更新/重要变化/每周Jay汇总。Codex新增权限内本地角色简报和公开来源feed；内部数据不进入公开feed，不调用新的外部AI，不新增数据库/角色权限。首批仅公开候选品牌与2条有日期官方动态。408项回归与语法通过，合成浏览器核验Jay/Grace角色切换和来源链接；生产待发布。当前任务heartbeat grace-brian-jay已创建ACTIVE（每日09:00 Asia/Shanghai；周一周报），依赖本机运行，首个定时触发尚未观测。内部周报在本地和当前任务交付，不是CRM后台持久周报；不宣称全天候实时监控。回滚撤销前端提交并暂停自动化，不改客户数据。详见docs/agent-intelligence.md。

PR76已发布：合并b64d1ed3f40e5a967e6da963f96e903a83e50df3，Pages35814738990成功，生产HTML/两模块/CSS/公开feed五份字节一致。真实登录确认信息简报、角色、来源正常，未发送AI请求或修改业务数据。验收补充：对产品字段中客户类型标签增加明确质量提示，并补充两家门业公开候选来源；无内部经营数据入库。每日调度仍待首次触发，不等于服务器常驻采集。

## 2026-09-23 三位智能体问答与知识增强（发布候选）

用户明确优先回答能力与业务知识并要求继续；执行Codex。新增按问题检索的证据回答、品牌过滤、主题追问、角色独立会话、来源/缺口和周期入口，修正上周及渠道有效率分母。仅使用当前账号已加载数据和公开feed，无新外部AI/权限/数据库/客户写入。合成浏览器已确认品牌限定、追问及Jay会话恢复；回归与语法在发布前复验。尚未部署。剩余限制：规则检索并非通用推理，公司战略/规格认证/价格/目标未自动确认，聊天只在当前页面保存。回滚本次前端提交即可，调度不变。

## 2026-09-23 OpenAI推理与语音候选（未部署）

授权人用户，要求接通通用推理和语音并指定统一OpenAI；执行Codex。完成独立服务端对话/转写/签名答案播报、前端模式/录音/停止/角色历史。保留CRM机密外发阻断，新增非机密主动对话范围等待明确批准；OPENAI_API_KEY尚缺，不设置策略开关。424回归、语法、合成浏览器通过，真实API及音频未验收。无生产权限/数据库/业务数据修改。具体发送字段、接口、限制及回滚见docs/agent-openai-voice.md；旧状态为本地规则分析，生产仍保持旧状态。

2026-09-23 OpenAI数据范围已获用户明确批准，OPENAI_API_KEY已配置，最新合成测试仍credit_balance_exhausted。追加本机Hello Grace/Brian/Jay唤醒、固定Hello Chloe问候、播报完成后恢复聆听、结束口令及10分钟关闭；不支持本机语言包时不回退云监听。428回归与语法通过，真实声音与语言包仍未验收。PR82继续未部署，待额度恢复；无需重复请求相同数据范围批准。


2026-09-23 智能体私人空间候选：用户要求选择智能体进入独立空间，调用推理并结合CRM分析。Codex已在PR82本地工作区实现独立空间、选中角色动态脑核、角色说明、返回大厅和既有分角色会话恢复；沿用精确身份权限。HTML/模块语法与11项相关回归通过，尚未完成浏览器视觉验收，未提交/部署。CRM本地分析保持；百炼推理适配与服务端脱敏汇总尚未实现，不宣称模型读取CRM。待明确百炼可发送的非机密文字/近期对话/录音以及权限范围内脱敏统计：周期、线索数、渠道及地区分布、阶段数量、转化率、逾期数量；不含标识、正文、报价、合同或付款。回滚本次前端修改即可，无生产数据/权限变更。


2026-09-23T14:03:06.177007+08:00 百炼适配候选完成：授权人项目负责人（本轮明确允许北京百炼非机密对话/录音/权限内脱敏统计，并要求背调分析和三种性别匹配音色）；执行Codex。固定北京qwen-plus/qwen3-asr-flash/qwen3-tts-flash，Grace Cherry女声/Brian Ethan男声/Jay Andre男声。新增服务器RLS下近30天最少统计及公开背调国家/类别分布，固定枚举、少于5分组抑制；不向模型发送身份、正文、金额、凭证或名单。三角色文字真实API通过，最终三音色中英TTS→强制HTTPS下载→ASR通过；434离线回归和语法通过，合成浏览器独立空间/返回检查通过。前状态OpenAI候选未部署，后状态百炼候选未部署；未设置BAILIAN_AGENT_POLICY。未完成：真实JWT统计查询/麦克风唤醒与生产上线验收。回滚撤策略并回退前端/函数、保留审计；残余限制：软限流非原子、样本非市场总体、固定近30天与看板周期不同。详见docs/agent-bailian-voice.md。
