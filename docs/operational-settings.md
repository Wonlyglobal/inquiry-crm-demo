# 系统设置第一批：2026-09-22

来源：用户接受优先四项后要求“开始做”；执行 Codex。沿用基线1.1工程负责人批准规则。

- 区域维护：老板可维护在职正式业务员；主管只维护现有业务管理范围，市场只读。复用 sales_target_people；影响区域推荐和目标看板区域汇总，不改变账号team、角色、客户归属。Chloe独立360授权不转为业务管理权限。
- 分配规则：保持分区优先、大区其次、同区姓名排序。新增暂停推荐；只过滤智能推荐，不阻止主管人工指定。设置加载失败时推荐报错，不能忽略暂停状态。人工分配仍用原有权限/确认流程。
- 跟进提醒：本人默认间隔1–90天、提前0–1440分钟、优先级；询盘跟进表单显式点击使用默认计划，最终保存仍走既有RPC。已有时间不覆盖，异步加载期间切换询盘不填入错误对象。任务快捷表单仍采用其现有手动时间；首响30分钟、报价7天、保留期规则没有改动。
- 邮箱：授权目录增加个人最近同步/测试时间，不返回错误详情、配置、凭证或邮件内容。共享邮箱排除。
- 新增私有表拒绝authenticated直写，受控RPC校验身份/管理范围/正式数据；更新带版本校验和审计。旧销售目标编辑API未在本轮重构。

验证：346项原回归及2项默认计划交互测试通过；PGlite隔离SQL验证角色、跨团队拒绝、停用/测试账号拒绝、并发版本、审计、本人默认值隔离和共享邮箱排除；生产只读预检零重复人员映射/个人邮箱，字段及审计结构匹配。

部署顺序：数据库新RPC → assignment-recommendation函数 → 页面。未提交真实客户操作或改动任何成员业务配置。发布和真实页面验证另记部署日志。
回滚：撤回页面及推荐函数，再撤销新RPC执行权；保留配置和审计。若已有人修改负责区域，应按审计由有权人员显式恢复，不能删除记录。
第一批不包含备用承接人、可编辑全局SLA、公海回收、其他推荐中的渠道/物料/AI/题库等设置；后续分别建设，不能把页面文案当作已实现功能。
