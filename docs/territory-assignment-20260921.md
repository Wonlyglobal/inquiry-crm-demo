# 按负责区域推荐业务员

用户2026-09-21提供12人表，要求更新CRM并在分配时显示最匹配区域和人员。精确人员资料见sales-territory-roster-20260921.json。

## 已完成生产资料更新

授权人：用户本轮明确要求；执行人：Codex，经已登录Supabase SQL Editor；日期2026-09-21。核对10个账号ID及中文名后，仅更新英文名和职务；12人资料写入既有sales_target_people，缺账号两人profile_id=null，英文名保留在来源备注。未创建登录账号、改变role/team/active、修改历史目标、分配客户或发信。李浩东业务资料归海外业务部，安全team仍是海外工程部，待用户确认部门及必要独立权限复核。邓盛文/Amanda、李亚莹/Lily待用户提供公司邮箱及账号关联。

更新与before/after审计同事务提交；回读12行确认。审计动作sales_roster_metadata_updated和sales_territory_updated，actor_id=null，reason注明用户授权、执行人及用途。归档迁移可幂等重放，不重复写相同资料与审计。回滚需按本次审计before_data校验after_data后恢复业务字段并追加回滚审计，不动权限或删除审计；新增未关联档案应先确认无后续引用再归档，不直接删除。

## 推荐规则候选，尚未发布

先比较询盘国家与已配置负责区域：分区精确匹配优先于大区覆盖，之后比较产品经验、在手量、待回复、首响与连续性。不把历史国家经验当作负责区域。不匹配人员不进入推荐前三；无人匹配、未设置、未知国家明确提示。中亚默认五国，俄罗斯、土耳其等未确认归属保持待确认；名单外国家不会猜测。此国家表为业务路由初版，后续需根据公司覆盖范围扩展。

前端显示国家、区域、英文名、负责范围和匹配依据，移除概率式置信度。旧服务版本不会被当作新规则结果；未匹配或未知区域人工分配有确认提示和必填原因。仍由人通过原RPC最终分配，数据库权限不变。服务端审计保留旧confidence字段为0，并标记confidence_not_estimated，不当作概率。

更新只影响分配推荐；重新分配保留人工选择及区域标签，不执行新自动推荐。主管/老板鉴权及询盘RLS查询继续使用原路径，未扩大可见范围。不存在账号/停用/非销售人员不会成为可分配人选。

## 验证及发布

318项Node离线检查通过；7项隔离PGlite断言验证12个资料、10个账号元数据、22条审计、无权限和账号新增、重复执行幂等。浏览器模块及去类型后的Edge模块语法通过；diff检查通过。未执行真实客户分配或对外邮件；未完成生产端新推荐验收。

须独立审核后发布页面与assignment-recommendation Edge Function（含_shared/sales-territory.mjs），人员数据已经更新，无需重复生产数据写入。仅合并Pages不足以发布Edge Function。推荐代码回滚为受保护PR回退和函数回退，不能撤销已由人工完成的分配。
