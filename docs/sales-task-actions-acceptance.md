# 任务中心第一阶段：直接处理

日期：2026-09-20。状态：2026-09-20 已发布生产；页面版本与匿名登录页验收通过，未执行真实客户写入验收。

## 交付

- 首页任务和今日工作台接入统一处理面板，重新读取当前本人询盘，展示客户、需求、最近跟进、触发原因和规则化下一步。
- 直接登记实际跟进、客户反馈和未来的下次计划，复用 `record_inquiry_followup_v2`；保存失败保留输入，保存成功锁定提交。
- 今日工作台保留跟进记录 ID 和邮件 ID；单独确认原跟进完成，或打开指定客户来信的回复编辑器。完成原任务复用 `complete_follow_up_task`，不批量关闭同客户其他任务。
- 提供原文、报价和完整询盘入口；首页增加全部任务入口。
- 打开面板不调用外部 AI、不发送消息、不修改首响判定。主管与市场角色不能使用此业务员操作入口，写入权限仍由既有服务端工作流执行。

## 验证

- 专项 23/23，全量 Node 测试 290/290；页面 module 语法及 diff whitespace 检查通过。
- 新增运行型测试覆盖未来时间、空内容、方法校验、角色拒绝、失效商机拒绝；既有测试中包含源码断言，不等同端到端验收。
- 隔离无头 Chrome 使用合成数据、阻断页面网络，验证失败保留输入、显式重试、重复提交拦截、完成指定原任务及 390px 手机无横向溢出；检查手机截图。
- 可重放脚本：`tests/sales-task-actions.browser.cjs`，运行前通过 `CRM_PLAYWRIGHT_MODULE` 指定 Playwright 包路径，`CRM_CHROME_EXECUTABLE` 指定 Chrome；`CRM_QA_OUTPUT` 指定截图目录。默认页面请求全部阻断，数据库为内存桩。
- 未验证真实生产写入、真实发信或全站真实登录会话；未执行数据库迁移。

## 后续

本阶段完成任务中心的直接处理入口。采购需求单、报价异议闭环、客户统一档案尚未实施。WhatsApp 仍通过原沟通入口处理；改期当前采用新增下一次计划并单独完成原任务，未新增批量改期或一键合并事务。

现有工作树已有风险中心和角色测试改动，本次保留且未发布，不把这些改动算作本阶段交付。修改前页面备份在 `/private/tmp/crm-task-center/before.html`，可对比提取本阶段补丁，回滚时不得覆盖其他在途改动。

## 2026-09-20T09:46:39+08:00 — 任务中心第一阶段生产发布

- 授权人：本次用户，明确指令“发布生产”；执行人：Codex；目标：Wonlyglobal/inquiry-crm-demo main / https://crm.foreverdoodle.com/；原因：上线已完成的任务中心直接处理。
- 发布前：c77d3ed797f515998a3dad57379f8f89396a015e；线上页面与该提交 SHA-256 完全一致。发布后：8d8838275b8a07c259658c644360eb1681173d8f。
- 范围：任务处理面板、指定来信回复入口、实际跟进与下次计划、指定原任务完成、全部任务入口；从最新生产仓库独立提取，未包含风险中心或其他在途改动。无数据库迁移、权限变更、真实客户发信或生产测试数据写入。
- 验证：独立发布树 283/283 测试、模块语法、diff 检查及合成数据浏览器验收通过；此前 290 项包含 7 项未发布风险中心测试。
- 部署证据：GitHub Pages workflow 35482196386 completed/success；线上 HTML 与发布文件字节一致，SHA-256 dc277fba47da46ec8bfdc086fa8c4a43aedb30f17d18b518b7b09b3c6cac32c3。全新匿名 Chrome 打开线上 HTTP 200，登录表单可见，页面错误 0。
- 回滚：对 main 执行本次提交的 git revert 并重新发布；无需数据库回滚。独立发布目录 /private/tmp/crm-task-release-20260920；原工作目录未重置，保留其他在途改动。
- 剩余：未使用真实员工账号执行生产写入验收；采购需求单、报价异议闭环、客户统一档案仍待后续阶段。
- 部署状态：任务中心第一阶段已发布生产并完成页面版本核验；不代表其他风险治理能力已上线。
