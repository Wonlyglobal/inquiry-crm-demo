services:
  mail-sync:
    build: .
    restart: unless-stopped
    env_file: .env
    healthcheck:
      test: ["CMD", "node", "-e", "process.exit(Date.now()-require('fs').statSync('/tmp/healthy').mtimeMs<180000?0:1)"]
      interval: 60s
      timeout: 5s
      retries: 3
