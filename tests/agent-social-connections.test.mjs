import test from 'node:test';import assert from 'node:assert/strict';
import {connectionReadiness} from '../integrations/social-summary/supabase/functions/social-summary/connections.mjs';
test('API key does not imply private analytics and values never escape',()=>{const env={YOUTUBE_API_KEY:'do-not-expose',FB_PAGE_TOKEN:'do-not-expose',IG_ACCESS_TOKEN:'do-not-expose',IG_BUSINESS_ID:'do-not-expose'};const x=connectionReadiness(k=>env[k]);assert.equal(x.platforms.youtube.private_analytics,'oauth_not_configured');assert.equal(x.platforms.facebook.page_token_used_by_current_sync,false);assert.equal(x.platforms.instagram.private_analytics,'insights_permissions_unverified');assert.doesNotMatch(JSON.stringify(x),/do-not-expose/)});
test('complete OAuth configuration is not authorization proof',()=>{const x=connectionReadiness(k=>k.startsWith('YOUTUBE_'));assert.equal(x.platforms.youtube.private_analytics,'authorization_scope_unverified');assert.equal(x.platforms.tiktok.user_authorization,'not_checked');assert.equal(x.platforms.linkedin.connector,'not_integrated')});
