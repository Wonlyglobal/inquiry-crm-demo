# NRS数字评分 v3

用户明确确认采用NRS 0—10分。沿用五类评价人、48道角色行为题、每份4道简答、月度评分与季度汇总、2026Q3起点和既有权限。

新问卷每题0—10整数：0严重未达、6基本达到、10持续显著超出；“未观察”单独选项，不计分，每维度至少2道有观察。0—4和9—10需逐题8字事例，简答规则不变。不是NPS推荐值，不计算推荐者减贬损者比例。

逐题、维度及本次评价保留0—10原始分与360-nrs-v3版本；旧记录不修改。汇总时v3除2归一到原5分基数，沿用原60/40和分组权重，8/10与旧4/5具有相同满分占比贡献。旧锁定结果不重算，新旧均分仅在相同满分基数汇总。题库改变并不意味着不同版本的测量含义完全相同，保留版本区分。

三函数迁移前检查生产定义哈希；提交校验拒绝旧网页版本并提示刷新。新增分数约束按题库版本区分0—10与1—5；无新增数据可见范围。调用方：直接评价、任务评价共用提交RPC；calculate_sales_360_cycle负责归一，不由前端计算综合得分。顺带修复现有percent_rank浮点数导致round(double precision,integer)不存在的计算错误：排名值转numeric，计算口径不变。

验证：隔离PostgreSQL覆盖所有角色、0/10端点、极端事例、NA覆盖、旧客户端拒绝、匿名拒绝、重复提交、真实汇总函数混合旧4/5与新8/10得32/40、全0得0/40、全10得40/40、旧记录保留及锁定禁止重算。浏览器合成测试验证0分事例框、NA覆盖、提交参数及手机布局；离线回归与语法通过。没有提交真实成员评分。

本文件为候选，未部署。回滚必须页面和数据库配套：恢复v2校验/提交函数与页面，保留NRS响应与0—10版本约束，并保留NRS汇总归一逻辑以正确读取已保存v3数据；不得盲目恢复1—5全局约束或删除新记录。需项目负责人单人批准发布，无需第二人审核。
