# 智能体世界接入

2026-09-23，用户明确“正式接入CRM”，随后提供金色WONLY标识并要求下接CRM形成整体。实施Codex。以main/77b55d7为初始来源，保留现有权限、统计、审批、AI外发阻断与接口。

新增三色动态全息脑核页面，标题“欢迎进入王力CRM系统”。Grace营销增长、Brian销售、Jay经营决策；点击进入复用当前CRM数据助手和按营销/销售/综合过滤的证据建议。暂不新增自主Agent执行、语音、定期汇报调度或新的AI模型服务。

按用户提供的既有专用入口约定，切换仅对已认证李铧燕身份显示：auth与profile ID同时匹配c43bd3c2-6e3a-4228-99c7-dc95f33643f2、认证邮箱chloelee@wonlyglobal.com、active及owner角色。直接导航和交互再次校验。该限制为产品界面准入，不是新数据权限；静态资源本身公开可下载。所有业务读取/写入仍由既有RLS、RPC和Edge接口强制授权。未新增后台权限或迁移。其他成员保留原有AI分析入口，仅改称智能体。

金色Logo来自用户提供文件，保存为assets/wonly-gold.png，原图不变。顶部与侧栏统一上下组合的WONLY/CRM标识。

验证：404项离线回归、浏览器模块语法通过；合成身份下基于真实CRM结构测试三角色入口、聊天面板复用、建议域、返回页面、撤销资格后拒绝和手机无水平溢出。真实业务对客操作未执行。上线状态见DEPLOY_AUDIT最终记录。

回滚：回退本次页面和assets提交，不改数据库；不会删除已有建议审计。剩余限制：三位智能体复用已有数据助手，不是独立自主模型；既有外部AI数据政策会拒绝未分类客户上下文，回退CRM可验证本地统计不代表外部模型已启用。自动汇报与语音未建设，不宣称已上线。
