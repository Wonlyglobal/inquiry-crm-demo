# 风险修复第一批验收

日期：2026-09-20；授权人：本次用户（开始修复）；执行人：Codex；基准：0b59af6；部署状态：本地候选，未提交、未发布。

## 变更与原因

1. 新迁移 20260920110000：风险扫描、工作台和处置入口显式拒绝缺失/停用/空角色身份。按案件保存 risk_case_controls，原 risk_user_controls 保留聚合兼容接口。调用方已检索：get_my_risk_controls、工作台、review_risk_case 及前端风险展示；外部未知调用方仍需生产发布前核对。
2. 限制变更在用户维度串行化，解除单个案件不清除其他案件限制。根据不可变 contain/release 事件恢复曾被覆盖的限制；曾错误关闭但仍未解除的案件重开至 remediation，并追加 control_recovered 事件。来源缺失的活动限制使迁移中止，避免静默放开。
3. 邮件生成和事实检查使用共同的服务端询盘授权：老板、本人负责人、有明确相同团队的主管。去掉邮箱所有权对整个客户线程的授权。内容查询前先读取最小对象标识并校验。
4. 邮件生成/翻译、事实检查、邮件沟通总结三条路径默认阻断外部 AI。没有可信分级/审批存储时，未分级数据按 L3 处理。请求中的 approved/classification 不可绕过，事实检查在读取缓存之前阻断，不伪造通过结果。这是临时遏制，不是完整 AI 审批产品；相关 AI 功能将暂停，AI 草稿的既有发送检查仍保留。
5. 新迁移 20260920111000：对 customer-documents、email-attachments、research-attachments 增加 restrictive SELECT 策略，与原权限策略取交集；下载或敏感揭示暂停、账号停用时无法经 authenticated 读取对象/生成新签名链接。未增加任何对象访问权限。

## 验证

- Node 回归：297/297 通过，其中新增主管对象访问、真实处理函数跨团队拒绝、授权用户 AI 默认阻断等行为测试。
- PostgreSQL：PGlite 0.5.8（npm 官方包，SHA-512 与注册表一致）隔离内存实例，22 项行为断言通过。覆盖缺失/停用/普通角色拒绝，历史双案件恢复，错误关闭重开，双案件解除顺序，聚合接口，真实 authenticated 存储 RLS 拒绝与恢复。
- 数据库测试使用合成 profiles/inquiries/storage 和扫描 stub，只验证本批函数与策略；不证明完整生产扫描、实际 Storage API 或多连接并发已验收。使用 PostgreSQL 语义，不以正则测试替代权限行为。
- git diff --check 通过。没有调用生产数据库、真实客户 AI、发送邮件或变更域名。

测试复现：在隔离目录安装精确版本 @electric-sql/pglite@0.5.8，将 PGLITE_MODULE 指向其 dist/index.js，用 Node 24 运行 tests/risk-controls-behavior.mjs。常规回归使用 Node 24 执行 node --test tests/*.test.mjs；新增 TS 导入依赖支持类型剥离的 Node 版本。

## 发布与回滚

先审核迁移恢复的案件清单及 AI 停用影响，再按两条迁移顺序应用，部署三条 Edge Function 及共享文件，复验受限账号无法通过 Storage API 获取新链接。需独立审核，当前缺少第二位审核人，未绕过分支门禁。

上线前回滚为撤销本批候选文件，不触及生产。上线后不得直接退回单案件控制或旧授权函数；保留新表和审计，采用向前修复。若需要关闭相关入口可先停止使用，但降低保护须另行批准。

## 尚未完成

- 服务端统一导出申请、主管/老板审批、生成和下载审计仍未建设；现有浏览器 Excel/PDF 不可宣称已安全收口。已下发浏览器的数据不能通过按钮禁用收回。
- 已签发的附件链接无法由该 RLS 变更即时撤销，现有有效期 60–120 秒。实际签名链接策略需生产隔离验证；未承诺即时撤销。
- AI 可信分级、审批例外及字段最少化网关仍需建设；其他 AI 入口不在本批三条路径内，需继续清单化整改。
- 生产前需核对实际函数/策略定义、桶是否私有、历史控制恢复结果及独立审核人。
