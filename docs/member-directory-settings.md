# 全员基本目录与本人邮箱状态

2026-09-22用户明确要求主管/市场系统设置显示全部成员，并指出已连接邮箱不应仍显示连接提示。

get_crm_member_directory为启用、生产、非测试的owner/sales_manager/marketing提供全体正式成员（含已停用）id、姓名、工作邮箱、CRM角色、团队、职务、启用状态及负责区域。不返回停用原因、登录/凭证、邮箱连接或客户内容。聚合多区域避免人员重复。固定search_path及匿名撤权；不修改profiles或客户RLS，也不增加写权限。

页面只请求本人personal邮箱status，区分已连接/异常/停用/未连接/查询失败。入口用于更新配置，仍只能操作本人。页面切换期间过期响应不覆盖其他模块按钮。

341项回归、浏览器语法、隔离PostgreSQL（授权角色、已停用成员、测试/staging排除、停用actor/匿名/业务员拒绝、底层RLS不变）通过。未提交真实账号或邮箱变更。迁移先于页面部署。回退页面后撤销新目录RPC执行权限，不修改其他数据策略。
