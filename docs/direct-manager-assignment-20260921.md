# 主管直接分配入口

2026-09-21；基于主分支 fefd7c8。用户要求优化主管直接分配体验。

询盘商机列表、看板询盘列表和详情顶部为主管/老板提供分配业务员或重新分配入口，复用 openDirectAssignment 与 assign_inquiry_to_sales。市场提交入口保留；业务员无分配入口。分配不需要开放主管的销售跟进/商业编辑权限。

分配前校验有效、无待审核无效申请、未关闭；后端既有 RPC 仍是权限边界。重新分配不调用仅支持待分配状态的智能推荐，必填人工原因，阻止重复选择原负责人，完成后刷新可见列表和详情。复用原邮件转发、通知、分配历史与审计流程；确认按钮明确包含邮件转发。本次不改变数据库、RLS、团队范围或实际客户归属。

验证：浏览器模块语法通过；既有310项离线回归通过；新增3项执行真实状态函数的角色/有效性/关闭状态检查通过；git diff --check通过。未执行生产分配或邮件转发，未完成浏览器端人工交互验收，未部署。

授权人：用户“优化”；执行人Codex。上线仍需独立PR审核。回滚：通过受保护PR恢复前端改动，无数据库回滚。当前Chloe临时主管角色系此前用户单独授权，本改动不更改该权限；录屏结束需恢复市场角色。
