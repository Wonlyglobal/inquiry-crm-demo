# 第三批：导出通知与报价打印收口验收

日期：2026-09-20。状态：仅本地候选，未提交、未合并、未部署。授权人：本次用户（“继续”）；执行人：Codex。目标：CRM 导出审批通知与报价详情；原因：补齐第二批审批可达性并收口未审批打印入口。依据：项目安全风险治理基线及前两批验收记录。

## 交付与前后状态

- 新增 `20260920130000_export_notifications.sql`，依赖第二批受控导出迁移和已有 notifications 表。申请时向独立老板及同团队主管生成站内通知；审批、驳回、撤回、领取状态通知申请人。缺少任一独立审批角色时提示申请人，继续保持待审批。
- 通知不携带客户内容、申请用途或导出快照。事件与接收人唯一约束防重复；客户端无法插入导出关联、修改关联内容或把普通通知改绑为导出通知；保留已读标记更新。不可变导出事件为权威审计记录，通知本身不替代审计链。
- 页面通知进入导出审批页面前重新读取权限受控列表，详情仍调用服务端权限校验。通知不授予权限；列表只返回最近 100 条，较旧通知可能回到审批中心。
- 报价“查看/打印”改为“查看明细”，删除应用直接打印代码，保留转义后的在线文本详情。单笔报价的正式打印审批尚未建设，因此候选版暂不提供该打印功能。无法阻止用户通过操作系统打印、截图或复制已经获准显示的数据。
- 前后生产状态不变；仅本地文件变更。没有生产迁移、真实通知、真实客户导出、真实审核或外部 AI 请求。

## 验证

- 301/301 Node 回归测试通过。
- 48 项隔离 PostgreSQL 导出行为断言通过（PGlite，合成账号与数据，加载实际迁移）。覆盖既有双审批/领取边界，以及通知接收范围、缺少审核人、无敏感内容、去重、只读内容、可标记已读、阻止客户端伪造/改绑、审批与领取通知。
- 页面模块 Node 语法检查、git diff --check 通过。本批未执行生产或真实浏览器通知验收；第二批的合成浏览器验收不能代替本批上线验收。
- 测试文件：tests/controlled-exports-behavior.mjs、tests/controlled-exports.test.mjs。临时 PGlite 环境仅位于 /private/tmp，不引入项目运行时依赖。

## 发布条件、回滚与剩余风险

发布前须独立审核，并核对生产 notifications 列类型、约束、RLS、触发器与所有现有写入路径。先应用依赖迁移和通知迁移，再发布使用新列的页面；否则通知查询会失败。需在隔离或可回滚环境验证各角色通知及报价查看行为。

未发布时可按差异撤销本批新增文件与页面变更，保留前两批候选。上线后优先向前修复，保留导出审计；不得恢复未经审批的直接打印/导出路径。通知故障与业务事件同事务，可能阻止相关导出操作，需要上线前兼容验收与告警。

独立审核人、生产兼容验收和部署仍未完成。定时催办/过期通知、正式单笔打印审批、多表 Excel/PDF、完整 AI 分级网关、外部防篡改归档与七年留存均不属于已完成能力。八条已盘点客户 AI 路径继续默认阻断。
