# 直接月度评分

用户于2026-09-21明确要求取消老板预先创建周期，保留月度评分和季度汇总。

主管/老板选择已结束月份，直接填写评分。新增 submit_direct_sales_360_evaluation 在同一事务内建立影子周期、被评人结果及该被评人的必要评价任务，然后调用原提交函数。打开表单不创建数据，提交失败全事务回滚。已存在周期的截止时间和状态不重置；重复提交不覆盖；不代替其他评价者打分。

权限沿用直属主管同团队或原有指定任务范围。拒绝非管理角色、停用/测试账号、测试对象及自评冒用。当前 Chloe team=市场部，与销售团队不匹配，本变更不会临时授予其跨团队评分权限。跨部门导出主管授权不复用于评分。

首次提交者身份保存在周期created_by供服务端审计；普通认证角色只能读取周期非身份字段，cycle_created事件不暴露首次评价人。现有业务发布、校准、申诉与奖金审批规则不变。

验证：329项离线回归；tests/direct-sales-360-postgres.mjs用PGlite合成库验证权限拒绝、原子回滚、月份边界、必要任务、重复提交、截止和身份列权限。可通过 CRM_PGLITE_MODULE 指定现有PGlite入口运行。生产函数创建事务回滚预演已通过（最终列权限收口另随迁移复验）。尚未部署，未提交真实评价。

回滚：页面回退；撤销或删除新增RPC，保留已提交评价与审计。为保护首次评分者身份，周期created_by列权限限制应继续保留，不能随意恢复整表SELECT。该变更不自动填全公司所有人的结果，正式计算前仍需完成目标及各必需评价组，未评分人员继续在季度表待评。
